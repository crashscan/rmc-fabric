# This directory is deprecated and should be removed
# All transport implementations have been moved to:
# - lib/dbus_transport_base/ (generic D-Bus substrate)
# - services/rmc-inventory/publishers/ (inventory-specific implementations)
#
# Keeping this CMakeLists.txt as a placeholder to prevent build errors.
# Remove this entire directory in a follow-up commit.
